# 🎴 全息殘影卡桌面掛件說明

> [!IMPORTANT]
> **本文件目的**
>
> `C:\cat` 底下的 `main.js`／`preload.js`／`electron-builder.yml` 是一個共用 Electron 外殼，讓任何一張（或好幾張）「全息殘影卡」html（`cards/jimmy_phantom_card.html`、`cards/gojo_phantom_card.html`…）變成桌面透明背景、永遠置頂的掛件，做法比照 `C:\question\desktop-pet`。卡片本體 html 都收在 `cards/` 資料夾底下，跟外殼程式碼（`main.js`/`preload.js`）分開放。
>
> **不動卡片本身**：任何一張卡片的 html 都不需要、也沒有被修改過。透明背景、右鍵拖曳這些效果都是外殼在執行期用 `insertCSS`/`executeJavaScript` 注入進去的，卡片檔案本身可以照樣單獨用瀏覽器開、正常顯示。

---

## 🚀 30 秒快速看懂（TL;DR）

> [!TIP]
> - 開發時預覽：`cd C:\cat && npm start`（預設跑 `package.json` 的 `cardFile`，目前是 jimmy）。
> - 包成 exe：`npx electron-builder --config build/<設定檔>.yml`，輸出在 `dist/`——或雙擊 `啟動html白名單管理.bat` 開 GUI，每份設定旁邊都有「📦 打包成 exe」按鈕，一鍵跑完看即時輸出，不用開終端機打指令。
> - 操作：滑鼠移到卡片上會旋轉（卡片自己原本就有的效果）、**按住滑鼠右鍵拖曳**可以移動卡片位置、`F9`（或該卡自訂的鍵）切換「可互動」/「點擊穿透」、`F10` 結束程式。
> - 想加一張新卡片：在 `build/` 底下複製一份 `.yml`，改 `productName`/`extraMetadata.cardFile`（或 `cards` 陣列裡的 `file`）就好，不用碰 `main.js`。

---

## ⭐ 核心重點總結

- ⭐ 視窗預設是「可互動」模式（能拖曳旋轉、右鍵拖曳移動），不是點擊穿透——跟 desktop-pet 的預設剛好相反，因為卡片沒有系統匣可以救援，先讓使用者摸得到比較不容易卡住。
- ⭐ 單卡模式的視窗大小刻意做成「跟卡片差不多大」（560×820），不是蓋滿整個螢幕：蓋滿螢幕時，兩個單卡 exe 同時開著，彼此「看不見但可以點」的範圍會整片重疊，右鍵容易點到疊在上面、但那個位置剛好透明看不見的另一張卡。
- ⭐ 多卡合一模式（一個 exe 同時擺好幾張卡）用 `<iframe>` 各自載入每張卡的 html，靠這個天然帳篷把每張卡的 `document`/`getElementById('card')` 隔開，不會互相打架，也完全不用改卡片程式碼。
- ⭐ 多卡模式一定要開 `nodeIntegrationInSubFrames: true`——Electron 的 `preload` 預設只套用到最上層 frame，沒開這個的話 iframe 裡的卡片會拿不到 `window.cardShell`，右鍵拖曳完全沒反應（卡片自己的 hover 旋轉效果不受影響，因為那是卡片自己的 `<script>`，這是實際踩過的雷）。
- ⭐ 卡片位置存檔跨重開機記得住：單卡模式存 OS 視窗座標（`window-position.json`），多卡模式存每張卡在共用視窗裡的座標（`card-slots.json`），都在該 exe 的 userData 資料夾底下（見〈疑難排解〉)。

---

## 🧭 快速導航

1. [🗂️ 檔案結構](#️-檔案結構)
2. [🎛️ 兩種模式：單卡 vs 多卡合一](#️-兩種模式單卡-vs-多卡合一)
3. [⚙️ package.json / extraMetadata 設定欄位](#️-packagejson--extrametadata-設定欄位)
   - [🖥️ npm start 快速切換：單卡 vs 多卡](#️-npm-start-快速切換單卡-vs-多卡)
4. [🕹️ 操作](#️-操作)
5. [➕ 怎麼加一張新卡片](#-怎麼加一張新卡片)
6. [📐 網格座標系統（目前的 10 卡佈局）](#-網格座標系統目前的-10-卡佈局)
7. [🏗️ 建置指令](#️-建置指令)
8. [🩹 疑難排解](#-疑難排解)
9. [📋 目前已有的建置設定 Cheat Sheet](#-目前已有的建置設定-cheat-sheet)

---

## 🗂️ 檔案結構

```
C:\cat\
├─ main.js               共用 Electron 外殼（單卡／多卡兩種模式都在這支）
├─ preload.js             contextBridge，只開放「移動位置」給卡片頁面用
├─ package.json           開發模式（npm start）用的預設設定，也是包裝時的基底
├─ electron-builder.yml   共用打包骨架（files 白名單、asar、portable target；
│                          本身不含任何卡片 html，一定要搭配 build/*.yml）
├─ build/                 每個「輸出版本」的小 override（extends 上面那份 yml，
│                          各自用 files 白名單列出自己真正要打包的 cards/*.html）。
│                          檔名由 builder-gui 自動產生／覆蓋（隨機四碼後綴），
│                          每次用 GUI 存檔都可能改名，下面是「目前」存在的兩份：
│   ├─ card-ez62.yml          單卡：吉米（jimmy），F9
│   └─ card-ehai.yml          多卡：六狗（feilong/wangcai/wuming/yinglang/
│                              zhuaige/jifeng 六張），共用 F9，F7 手勢任何張數都能
│                              用、F8 時鐘循環 3 張以上才會啟用
├─ dist/                  npm run dist / electron-builder 的輸出（portable .exe）
├─ cards/                 卡片本體 html，不被修改——jimmy_phantom_card.html／
│                          gojo_phantom_card.html／gojo_hollow_purple_card.html／
│                          六狗系列 feilong/wangcai/wuming/yinglang/zhuaige/
│                          jifeng_phantom_card.html／營區陽光男孩-幻影卡.html／
│                          dog_cards/（六狗卡片的「來源素材」assets/＋重新生成用的 build/ 食譜；
│                          原本還有一份跟上面逐位元組相同的 6 個 html 拷貝，已在卡片瘦身時移到
│                          card-slim/backup/dog_cards_dupes/，見〈🪶 卡片瘦身〉）
├─ card-fx/               卡片特效（稀有度切換／技能演出／卡背資訊／音效）：fx.css＋fx.js 是唯一原始碼，
│                          apply_fx.py 把它注入 cards/*.html（可重複執行）；cards_meta.json 放每張卡的
│                          編號／收藏日期／四項數值。見〈✨ 卡片特效〉
├─ card-slim/             卡片瘦身工具：slim.py 把卡片內嵌的 base64 圖片改成 WebP、重複的圖收成一份
│                          （可重複執行、原檔自動備份到 card-slim/backup/<時間>/）。見〈🪶 卡片瘦身〉
├─ card-export/           匯出分享（旋轉一圈錄成 WebM／GIF，或匯出 PNG）：exporter.js（main process：離屏視窗＋
│                          逐格截圖）、encoder.html/.js/-preload.js（隱藏視窗裡的 WebM／GIF 編碼器）、gifquant.js（GIF 量化＋寫入，Node 也能直接 require 做離線比對）。
│                          會被打包進 exe（electron-builder.yml 的 files 有 card-export/*）。見〈📤 匯出分享〉
├─ card-maker/            「圖片一鍵生成卡片」後端（Python）：make_card.py 去背→抽主色→
│                          推導配色/全息色階→填 card_template.py→輸出 cards/*.html。
│                          不會被打包進任何 exe；見〈🪄 圖片一鍵生成卡片〉
├─ builder-gui/           html 白名單管理 GUI（npm run builder-gui），獨立小工具，
│                          不會被打包進任何卡片 exe，見〈怎麼加一張新卡片〉
└─ 啟動桌面掛件.bat        雙擊即可用 node_modules 裡的 electron 跑（開發用）
```

---

## 🎛️ 兩種模式：單卡 vs 多卡合一

`main.js` 用 `package.json`（或 `build/*.yml` 的 `extraMetadata`）的形狀決定要走哪個模式：

| | 單卡模式 | 多卡合一模式 |
|---|---|---|
| 設定欄位 | `cardFile`（字串） | `cards`（陣列，每個 `{file, x, y}`） |
| 視窗大小 | 跟卡片差不多大（`windowWidth`×`windowHeight`，預設 560×820），置中在螢幕上 | 蓋滿整個螢幕（因為只有一個視窗，不會有「兩個視窗互搶滑鼠事件」的問題） |
| 卡片怎麼載入 | `win.loadFile()` 直接載入該 html，卡片本身就是最上層 frame | 動態產生一個 `stage.generated.html`，每張卡各自用一個 `<iframe src="file:///...">` 嵌進去 |
| 右鍵拖曳的實作 | `preload.js` 的 `moveBy()` 送 IPC 給 main.js，`win.setPosition()` 搬動整個 OS 視窗 | `moveBy()` 改用 `window.parent.postMessage()`，只搬動那張卡在共用視窗裡的 `.card-slot` 位置，不牽涉 OS 視窗 |
| 位置存檔 | `window-position.json`：`{x, y, zoom, opacity}` | `card-slots.json`：`[{x,y,zoom,opacity}, ...]`，索引對應 `cards` 陣列順序 |
| F9/F10 快捷鍵 | 每個單卡 exe 各自一組（同時開多個單卡 exe 時，每個要給不同的 `toggleKey`，OS 全域快捷鍵一次只能被一個程式搶到） | 整個視窗共用一組，因為只有一個行程 |

`preload.js` 的 `moveBy()` 用 `window.top === window.self` 自動判斷現在是哪一種情境，卡片自己的拖曳偵測腳本（`main.js` 裡的 `INJECTED_SCRIPT`）完全不用知道、也不用寫兩份。

---

## ⚙️ package.json / extraMetadata 設定欄位

這些欄位可以寫在根目錄的 `package.json`（給 `npm start`/`npm run dist` 用），也可以寫在 `build/*.yml` 的 `extraMetadata` 底下（electron-builder 打包時會合併進封裝後的 `package.json`）：

| 欄位 | 說明 | 預設值 |
|---|---|---|
| `cardFile` | 單卡模式：要載入哪個 html | `null` |
| `cards` | 多卡模式：`[{file, x, y, zoom?, w?, h?}]`，有這個欄位就會走多卡模式（優先於 `cardFile`） | `null` |
| `toggleKey` | 切換互動/穿透的全域快捷鍵 | `"F9"` |
| `quitKey` | 結束程式的全域快捷鍵 | `"F10"` |
| `resetKey` | 重設卡片位置的全域快捷鍵（單卡、多卡都適用，按下去一樣會先跳確認視窗，不會無聲重設） | `"F11"` |
| `autoFlipKey` | 「每秒自動翻面循環」開關的全域快捷鍵（單卡、多卡都適用；所有卡片都有 `toggleAutoFlip()`）。⚠️ 不能設成 `F12`：Windows 把它保留給除錯器，`globalShortcut` 會註冊失敗 | `"F4"` |
| `skillKey` | 技能演出（蓄力→爆發）的全域快捷鍵（單卡、多卡都適用） | `"Ctrl+Alt+1"` |
| `rarityKey` | 切換稀有度 SR→SSR→UR→LR 的全域快捷鍵 | `"Ctrl+Alt+2"` |
| `muteKey` | 音效靜音／取消靜音的全域快捷鍵 | `"Ctrl+Alt+3"` |
| `zoomInKey` / `zoomOutKey` | 放大／縮小卡片的全域快捷鍵（單卡、多卡都適用；多卡時對全部卡片一起調） | `"Ctrl+Alt+="` / `"Ctrl+Alt+-"` |
| `opacityUpKey` / `opacityDownKey` | 更不透明／更透明的全域快捷鍵 | `"Ctrl+Alt+]"` / `"Ctrl+Alt+["` |
| `exportKey` | 匯出分享的全域快捷鍵（單卡、多卡都適用；多卡＝匯出**游標所在的那張卡**） | `"Ctrl+Alt+4"` |
| `exportFormat` | 快捷鍵匯出的格式：`webm`／`gif`／`png`（系統匣選單可另選任一種） | `"webm"` |
| `exportSeconds` | 影片／GIF 旋轉一圈的秒數 | `3` |
| `cycleKey` | 時鐘式循環開關的全域快捷鍵（只有多卡合一、**3 張以上**才有意義） | `"F8"` |
| `exportLayoutKey` | 多卡專用：匯出**整個版面**（所有卡一起，合成一支 WebM／GIF／PNG）的全域快捷鍵，格式跟 `exportFormat` 一樣；時鐘轉圈動畫只放在系統匣選單 | `"Ctrl+Alt+5"` |
| `gestureKey` | 手勢辨識開關的全域快捷鍵（只有多卡合一模式有意義） | `"F7"` |
| `skeletonKey` | 手部骨架視覺效果開關的全域快捷鍵（只有多卡合一模式有意義） | `"F6"` |
| `windowWidth` / `windowHeight` | 單卡視窗、多卡模式每個 `.card-slot` 的預設格子大小 | `560` / `820` |
| `defaultOffset` | 單卡模式：第一次啟動、還沒被拖過時，相對螢幕正中央的偏移量 `{x,y}` | `{x:0,y:0}` |
| `ambientStars` | **環境星光**：卡面上常駐的小星星（card-fx 的 `.fx-stars` 層）要不要顯示。**預設 `false`＝不顯示（card-fx 加進來之前「以前」的樣子）**；builder-gui 的「✨ 環境星光」勾選框勾了才會寫成 `true`。純外觀選項，單卡、多卡都適用，匯出的影片／GIF／PNG 也照這個設定 | `false` |
| `launchAtLogin` | 開機（Windows 登入）自動啟動這個掛件的**預設值**：只在這個 exe「第一次啟動、還沒在系統匣表態過」時套用一次，之後以系統匣選單的「🚀 開機自動啟動」勾選為準（存在 userData 的 `settings.json`）。builder-gui 有對應的勾選框 | `false` |

開發時也可以用環境變數暫時覆蓋，不用真的去改 `package.json`：

**PowerShell：**

```powershell
$env:CARD_FILE = "cards/gojo_phantom_card.html"; npx electron .
$env:CARDS_JSON = '[{"file":"cards/jimmy_phantom_card.html","x":80,"y":80}]'; npx electron .
```

**cmd.exe：**（`set "VAR=值"` 這個帶外層雙引號的寫法才能正確吃進值裡面 JSON 本身的雙引號，不能省略；不加外層引號的 `set VAR=值` 在這個案例其實也能動，但少了這層保護，值裡如果不小心多打一個空白字元就會整個跑掉）

```bat
set "CARD_FILE=cards/gojo_phantom_card.html" && npx electron .
set "CARDS_JSON=[{"file":"cards/jimmy_phantom_card.html","x":80,"y":80}]" && npx electron .
```

> [!NOTE]
> 這兩個環境變數設定後會留在**這個終端機 session**裡，之後在同一個視窗打 `npm start` 也會沿用，不會自動消失。用完想改回單純吃 `package.json` 的設定，關掉終端機重開；或者 PowerShell 用 `Remove-Item Env:CARD_FILE`／`Remove-Item Env:CARDS_JSON`，cmd.exe 用 `set CARD_FILE=`／`set CARDS_JSON=`（等號後面留空＝刪除該變數）清掉。

### 🖥️ `npm start` 快速切換：單卡 vs 多卡

`npm start` 只讀根目錄 `package.json` 目前的 `cardFile`／`cards`——那是**上次用 builder-gui 存檔、或手動改 `package.json` 留下的狀態**，不是固定的，所以「`npm start` 打開來是哪張卡/哪組合」會隨上次動過什麼而變。想確認或暫時切換，不用去改 `package.json`：

**單卡模式**（例如吉米）：

```powershell
# PowerShell
cd C:\cat
$env:CARD_FILE = "cards/jimmy_phantom_card.html"
npm start
```

```bat
:: cmd.exe
cd C:\cat
set "CARD_FILE=cards/jimmy_phantom_card.html"
npm start
```

**多卡合一模式**（六狗，目前 `build/card-ehai.yml` 這份設定的排法；F7 手勢（任何張數的多卡都能用；2026-09-20 查證更正，舊說明寫「剛好 6 張」是過時的）；F8 時鐘式循環 **3 張以上都能用**（2026-09-20 起，見下面「時鐘式循環」），見〈手勢散開/收縮〉相關程式碼註解）：

```powershell
# PowerShell
cd C:\cat
$env:CARDS_JSON = '[{"file":"cards/feilong_phantom_card.html","x":101,"y":-153},{"file":"cards/wangcai_phantom_card.html","x":671,"y":-153},{"file":"cards/wuming_phantom_card.html","x":1241,"y":-153},{"file":"cards/yinglang_phantom_card.html","x":101,"y":379},{"file":"cards/zhuaige_phantom_card.html","x":671,"y":379},{"file":"cards/jifeng_phantom_card.html","x":1241,"y":379}]'
npm start
```

```bat
:: cmd.exe（set 後面那組外層雙引號一定要留，見上面那則 NOTE）
cd C:\cat
set "CARDS_JSON=[{"file":"cards/feilong_phantom_card.html","x":101,"y":-153},{"file":"cards/wangcai_phantom_card.html","x":671,"y":-153},{"file":"cards/wuming_phantom_card.html","x":1241,"y":-153},{"file":"cards/yinglang_phantom_card.html","x":101,"y":379},{"file":"cards/zhuaige_phantom_card.html","x":671,"y":379},{"file":"cards/jifeng_phantom_card.html","x":1241,"y":379}]"
npm start
```

> [!TIP]
> 這組六狗座標是從 `build/card-ehai.yml` 的 `extraMetadata.cards` 直接複製來的。如果之後透過 builder-gui 調整過六狗的格位，這裡貼的座標就會過期——用當下 `build/` 底下那份「六狗」`.yml`（`productName: "六狗"`）的 `extraMetadata.cards` 為準，複製貼上取代掉上面 `CARDS_JSON` 的內容即可（GUI 存檔會自動重新產生這份 `.yml`，見〈疑難排解〉上面那則 GUI 存檔的警語）。

---

## 🕹️ 操作

- **滑鼠移到卡片上**：卡片會朝滑鼠方向傾斜——這是卡片自己原本就有的 hover 效果。
- **點擊穿透模式下的全域滑鼠追蹤**：按 `F9` 進入穿透後，視窗本身收不到滑鼠事件，外殼改用 `screen.getCursorScreenPoint()` 每 33ms 輪詢游標座標（只在穿透模式才跑、游標沒動就不送），換算成視窗座標後由注入腳本翻譯成卡片原本就在聽的 `pointermove`／`pointerleave`，所以卡片在穿透時**依然會跟著游標傾斜、流光**，連游標在視窗外面也追得到（離卡片中心超過約 1.6 個卡片寬/高才視為離開、回到閒置擺動）。單卡、多卡都適用，卡片程式碼完全不用改。多卡模式由 stage 頁面依每個 iframe 的位置換算後 `postMessage` 進各卡。
- **在卡片上按住滑鼠右鍵拖曳**：移動這張卡的位置（放開後自動存檔）。
- **`F9`（或該建置設定的 `toggleKey`）**：切換「可互動」／「點擊穿透」（穿透時滑鼠會直接穿過去，可以點到桌面上被卡片蓋住的圖示/視窗）。
- **`F10`（或 `quitKey`）**：結束程式。
- **`F11`（或 `resetKey`）**：重設卡片位置——跟系統匣選單的「↩️ 重設卡片位置」是同一個動作，一樣會先跳確認視窗，不會按一下就無聲清掉手動排過的位置（單卡、多卡都適用）。
- **`F4`（或 `autoFlipKey`）**：開始/暫停「每秒自動翻面循環」（預設卡片靜止；拖曳時暫停、放開後接續）。**所有卡片都支援**（吉米、五条悟兩張、營區陽光男孩已補上卡背＋雙擊翻面＋`toggleAutoFlip()`），也可以從系統匣選單操作。這是全域快捷鍵、由外殼轉發進卡片（單卡：`executeJavaScript`；多卡：stage 頁面 `postMessage` 給每個 iframe，再由 `INJECTED_SCRIPT` 呼叫卡片掛在 `window` 上的 `toggleAutoFlip()`）——卡片自己用瀏覽器單獨打開時則是按 `L` 鍵。故意不讓 exe 用 `L` 當全域鍵：會搶走使用者在其他程式打字的按鍵；卡片內的 DOM 鍵盤事件則要該 iframe 有焦點才收得到，透明置頂視窗幾乎不會有焦點。
- 多卡合一模式另外還有 **`F7`（或 `gestureKey`）** 開始/暫停手勢辨識、**`F6`（或 `skeletonKey`）** 開啟/關閉手部骨架視覺效果；3 張以上的多卡合一還有 **`F8`（或 `cycleKey`）** 開始/暫停時鐘式循環。
- **5 卡「中央＋四角」版面與環繞（2026-09-20）**：剛好 5 張時，F8 改成**只讓四張繞著第 1 張轉**（`computeCycleConfigCentre()`）：第 1 張（清單第一張）固定在中央不動；其餘 4 張以第 1 張的中心為圓心、橢圓 rx／ry＝四張到中心的平均水平／垂直距離（預設版面 570／266），角度依目前位置換算、排序後每隔 90° 均分（站四角＝45／135／225／315°）；轉到中央卡**下半部**的那張蓋在它前面、**上半部**的躲到後面（`z-index` 前後遮擋，舞台頁面與匯出都一樣），看起來像真的繞著它轉。翻面（F4／雙擊）、手勢、縮放、拖曳等邏輯完全沒動。GUI：多卡編輯區「🎴 一鍵排成 5 卡（中央＋四角）」按鈕——四個角＝6 卡版面的四個角（x=101／1241、y=-136／396），第 1 張在兩排正中間（x=671、y=130，卡片中心剛好在上下兩排中點）；順序：中央、左上、右上、右下、左下（順時針）；不夠 5 格時優先補還沒用過的卡片，超過會裁掉；GUI 的時鐘預覽也是「中央不動、四張繞它」。⚠️ 你原本的「露營區」yml 是手調座標（中央 674,180），剛好 5 張所以**重新打包後**環繞行為會變成這樣（原本是 5 張一起繞）；想要標準版面就在 GUI 按新按鈕。其他張數（3、4、7、10…）維持原本「全部一起繞」。
- **時鐘式循環（F8）任意張數（3 張以上）**（2026-09-20，原本限定剛好 6 張——5 張的「露營區」按 F8 沒反應就是這個原因，不是壞掉）：**6 張**沿用原本「上三下三」的固定角度（10／12／2 點與 8／6／4 點，橢圓取自上下兩排的座標，行為完全不變）；**其他張數**用 `computeCycleConfigAny()`：依每張卡目前的位置換算成鐘面角度、依角度排序後**平均分配 360°/N**（例如 5 張＝每張間隔 72°），橢圓中心與半徑由所有卡中心的範圍決定（半徑至少 150px，避免全部排成一列時半徑是 0）；轉一圈秒數仍是 `cyclePeriodSeconds`。GUI 的「▶️ 播放動畫預覽」也同步支援 3 格以上（`computeCycleConfigAnyPreview`，跟 main.js 同一套算法）。少於 3 張不啟用。要有「上排三張下排三張」以外的漂亮軌道，把卡片大致排成橢圓形即可。
- **`Ctrl+Alt+1`（或 `skillKey`）**：技能演出。**`Ctrl+Alt+2`（`rarityKey`）**：切換稀有度。**`Ctrl+Alt+3`（`muteKey`）**：音效靜音開關。三個都在系統匣選單裡也有，詳見〈✨ 卡片特效〉。點一下卡片本身也會施放技能（雙擊＝翻面，不會誤觸）。
- **縮放與透明度**：游標在卡片上**滾輪＝縮放**、**Alt＋滾輪＝透明度**、**滑鼠中鍵＝重設**（多卡合一時只調滑鼠所在的那一張）；也可以用全域快捷鍵 `Ctrl+Alt+=`／`Ctrl+Alt+-` 放大縮小、`Ctrl+Alt+]`／`Ctrl+Alt+[` 更不透明／更透明（多卡時對全部卡片一起調），系統匣選單有「🔍 縮放與透明度」子選單。縮放範圍 0.25～2.5 倍（相對 `cardScale` 那個基準）、透明度 0.2～1，**依卡片中心縮放**；設定**跟位置一起存檔**（單卡在 `window-position.json`，多卡每一格各自存在 `card-slots.json`），重開機記得住；`F11` 重設位置時會一併重設縮放與透明度。穿透模式（`F9`）下滾輪收不到，用快捷鍵。
- **每張卡各自的縮放（打包設定）**：GUI 每一格有「縮放 %」欄位（100＝跟「🔍 卡片整體縮放比例」一樣大，範圍 25～250），寫進 yml 的 `cards[].zoom`（倍率，等於 1 時不寫，例如 `{ file: cards/wangcai_phantom_card.html, x: 671, y: -136, zoom: 0.7 }`）。**以格位中心縮放**：yml 的 `x`／`y` 是「縮放前」的格位左上角，縮放只讓格位往中心縮／放，卡片中心位置不變（跟執行期滾輪縮放同一個補償算法）；實際大小＝整體縮放 × 這張卡的縮放。桌面掛件把它當成「這張卡的預設縮放」：第一次啟動就用它；`F11`「重設卡片位置」、系統匣「重設縮放與透明度」、滑鼠中鍵重設，都回到**這張卡自己的預設縮放**（不是 100%）。⚠️ 已經在那台電腦上拖曳過／縮放過的卡片會存在 `userData\card-slots.json`（存檔優先，跟座標同一個規則），要按 `F11` 重設才會套用新打包的縮放。單卡模式沒有這個欄位（本來就只有一張，用整體縮放）；六卡循環、手勢散開／收縮都以每格「縮放後」的實際大小算，不受影響。座標挑選器畫的還是未縮放的 560×820 格位方框（x／y 的定義沒變）。
- **匯出分享**：`Ctrl+Alt+4` 把卡片旋轉一圈錄成 WebM（可在 GUI 改成 GIF／PNG），完成會跳通知、點通知打開資料夾；多卡時先把游標移到要匯出的那張卡上再按；多卡另有 **`Ctrl+Alt+5`（`exportLayoutKey`）匯出整個版面**與系統匣的「🕐 時鐘轉圈動畫」。詳見〈📤 匯出分享〉。
- **實作備忘（縮放與透明度）**：縮放＝視窗依中心 `setBounds` ＋ CSS 變數 `--shell-scale`（＝ `cardScale` × zoom），透明度＝ `--shell-opacity`（套在 `body` 的 `opacity`，兩種模式同一套）；卡片的 `.stage` 現在用 `position:fixed` ＋ `translate(-50%,-50%)` 置中（原本靠 body 的 grid 置中，視窗比卡片布局小時會退成靠左上對齊，縮小後卡片會偏到右下），並隱藏滾動條。多卡的初始縮放/透明度放在 iframe 網址的 `#view=<縮放>,<透明度>`。
- **開機自動啟動**：系統匣選單的「🚀 開機自動啟動」勾選項目，切換後立即寫入／移除 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`（不需要系統管理員權限），勾選狀態每次開選單都重新向系統查詢，所以在工作管理員「啟動」頁面關掉它也會如實反映。portable 版記錄的是你實際雙擊的那個 exe（`PORTABLE_EXECUTABLE_FILE`），不是每次啟動解壓到暫存資料夾的那份；登錄值名稱用各 build 的 `extraMetadata.name`，同時裝好幾個掛件互不覆蓋。想完全移除：在系統匣取消勾選再刪 exe。
- **系統匣（工作列右下角）圖示**：每個 exe 啟動時都會建立一個，選單上每一項後面都會標出對應的快捷鍵（現在分成「✨ 卡片特效」〈技能演出／稀有度／靜音／自動翻面〉與「🔍 縮放與透明度」〈放大／縮小／更不透明／更透明／重設〉兩組，全部攤平在選單上，不用點開子選單；改了 GUI 或 `package.json` 的鍵，選單上的標示會跟著變），左鍵點一下等同按 `toggleKey`。不依賴全域快捷鍵，同時開好幾個 exe、快捷鍵互相搶不到的時候，這是唯一保證找得到的操作入口（見下面〈疑難排解〉）。 **選單不再被卡片蓋過**：`startKeepOnTop()` 每 6～10 秒會重新搶佔置頂（`setAlwaysOnTop`＋`moveTop`），以前會剛好把掛件視窗拉到正在開著的系統匣選單上面（實測重現：選單開約 17 秒後卡片畫在選單上、擋住左半邊）；現在選單開著（`menu-will-show`→`menu-will-close`）期間暫停重新搶佔、也不重建選單（匯出進度每 10% 會重建一次），關掉後恢復；另有 60 秒保險，收不到關閉事件時旗標會自己解除。
- **快捷鍵提示（含匯出分享）**：卡片下方那行操作提示，桌面掛件會**依實際設定的鍵改寫**——卡片自己寫的是單獨用瀏覽器開的按法（「按 L 鍵切換自動翻面」，在掛件裡 L 鍵沒有作用），掛件裡會變成例如「拖曳卡片自由旋轉 · 雙擊翻面看背面 · **F4** 自動翻面 · **Ctrl+Alt+4** 匯出分享」（改了 `autoFlipKey`／`exportKey` 會跟著變；不改任何卡片 html，瀏覽器單獨開的卡片維持原樣）。⚠️ **多卡版面（例如六狗）這行提示看不到**：上排卡片的提示被下一排卡片蓋住、下排的超出螢幕（列距只有 532px），所以另外提供系統匣選單「⌨️ 顯示快捷鍵提示（畫面上 10 秒）」，在畫面中央列出目前生效的所有快捷鍵（含匯出分享）。 **開關類快捷鍵的畫面提示（2026-09-20）**：按 `F9`（互動／點擊穿透）或 `F8`（時鐘循環）時，畫面上方會浮出一顆小膠囊——第一行是狀態（「👻 點擊穿透：已開啟」「🖱️ 互動模式：已開啟」「🕐 時鐘式循環：已開始」「⏸ 時鐘式循環：已暫停」），第二行是怎麼切回來；文字**淡入 0.28 秒 → 停留約 1.8 秒 → 淡出 0.7 秒**，滑鼠穿透、跟匯出進度面板是兩個獨立元素（匯出中按 F8／F9 不會蓋掉進度）。F8 的狀態由舞台頁面回報（IPC `cycle-state`），所以循環正在轉時**拖曳卡片造成的自動暫停**也會提示（「拖曳卡片時自動暫停，F8 繼續」）；沒在轉時拖曳不會誤提示。剛好 5 張時開始提示會註明「（中央不動，四張繞它）」。F9 從快捷鍵、系統匣選單、左鍵點系統匣圖示三種方式觸發都會提示。單卡、多卡都有（F8 只有多卡）。**`F4` 自動翻面、`F7` 手勢辨識、`F6` 手部骨架也有同樣的提示**（「🔄 每秒自動翻面：已開啟／已關閉」「🖐️ 手勢辨識：已開啟（布＝散開、拳頭＝收縮、剪刀＝翻面）／已關閉」「⚠️ 手勢辨識：無法開啟攝影機」〈紅色、F7 重試〉「🦴 手部骨架：已顯示／已隱藏」）：F7／F6 由舞台頁面回報實際狀態（IPC `toggle-state`，攝影機權限被拒時不會誤報「已開啟」）；F4 因為卡片 html 的 `toggleAutoFlip()` 不回傳狀態（不能改卡片），由 main.js 自己記錄開關（F11 重設、頁面重新載入會歸零為「關」）——⚠️ 若在卡片視窗裡直接按 `L` 鍵（卡片自己的自動翻面鍵）切換，外殼不知道，提示可能跟實際相反。系統匣選單點同一項也會提示。
⌨️ 快捷鍵面板現在列出**所有**生效的鍵（2026-09-20 補齊）：`F9`、`F8` 時鐘循環（剛好 5 張會註明「中央不動，四張繞它」）、`F7` 手勢／`F6` 骨架（多卡）、`F4`、`Ctrl+Alt+1～4`、`Ctrl+Alt+5` 匯出整個版面（多卡）、放大縮小、更不透明／更透明、`F11`／`F10`、滑鼠操作；系統匣 F8 項目同樣會註明，啟動日誌也有 `Ctrl+Alt+5` 這一行。提示文字放大到 14px 並加亮、加陰影；⌨️ 快捷鍵面板與匯出狀態面板整體放大約 1.5 倍（寬 460px、標題 26px、內文 18px）。卡片下方提示的高度仍固定 32px、多出來的行往下溢出，11 張卡實測卡片位置不變。
- **GUI 的動畫預覽（打包前就看得到）**：「座標預覽」（單卡的「開機位置預覽」與多卡的「座標定義與預覽」）上方有兩個開關——「▶ 翻面動畫預覽」＝每 1 秒翻面一次（跟掛件 `F4`「每秒自動翻面」同一套：每秒 +180°，逐格緩動 0.13，翻過去看得到卡背）；「▶ 旋轉一圈預覽」＝3 秒一圈、緩入緩出、上下搖擺 ±7°、轉完停 0.8 秒（跟「匯出分享」錄下來的旋轉一樣）。同一時間只播一種；作用在畫面上所有預覽卡，每格的縮放也會反映。六卡的「時鐘式循環」與手勢散開／收縮本來就有各自的預覽開關。⚠️ GUI 視窗被別的視窗完全蓋住時 Chromium 會暫停動畫（跟其他預覽動畫一樣），要看得到 GUI 才會動。

---

## ✨ 卡片特效（稀有度切換／技能演出／卡背資訊／音效）

所有卡片（含之後用「圖片一鍵生成」做的新卡）都內嵌同一份特效程式，**單獨用瀏覽器打開、或包成 exe 用桌面掛件都能用**。

| 功能 | 桌面掛件 | 瀏覽器單獨開 | 說明 |
|---|---|---|---|
| **技能演出** | `Ctrl+Alt+1`／點一下卡片 | `S`／點一下卡片 | 蓄力 0.75 秒（光環漸強、粒子向中心匯聚、卡片微縮並輕微顫抖）→ 爆發（閃光、雙層衝擊環、粒子四散、震動、殘影加倍、全息瞬間增亮）。在背面施放也會演出。 |
| **稀有度切換** | `Ctrl+Alt+2` | `R` | SR → SSR → UR → LR 循環。換的是：**邊框金屬感**（SR 銀／SSR 金／UR 稜鏡彩虹／LR 珍珠玫瑰）、**全息強度**（飽和度與亮度）、**星光密度**（10／24／48／72 顆，會閃爍；要在打包時勾選「✨ 環境星光」才會顯示，預設關閉）、UR/LR 另加**稜鏡層**、稀有度晶片材質。每張卡會記住自己的稀有度（localStorage）。 |
| **卡背資訊** | 翻面（雙擊／F4） | 雙擊 | 背面顯示 **編號 NO.**、**屬性**（晶片）、**稀有度**、**ATK／DEF／SPD／LUK 四項數值**、**收藏日期**。 |
| **音效** | `Ctrl+Alt+3` 靜音開關 | `M` | WebAudio 即時合成，**不需要任何音檔**：翻面（雙擊時）、稀有度切換（音階隨階級變長）、技能蓄力＋爆發。預設開啟；靜音狀態會記住。 |

**設計取捨**
- 自動翻面（F4）的每秒翻面**不發聲**——只有使用者自己的動作（雙擊、按鍵、點擊）才會出聲，不然每秒一聲會很煩。
- 多卡合一時：技能／稀有度**每張卡各自演出、各自切下一階**，但**只有第一張卡發聲**（十張疊在一起是噪音）；靜音只對第一張下指令，狀態靠 localStorage 讓其他卡同步。
- **環境星光可以整個關掉（預設關）**：小星星是 card-fx 才加的，以前的卡片沒有；`ambientStars`（GUI 的「✨ 環境星光」勾選框）為 `false` 時，外殼用一條 CSS（`.fx-stars{display:none}`，放在單卡／多卡共用的 `CARD_SCALE_CSS` 裡）把那一層藏起來。**只藏星星**：技能演出的粒子、稀有度的邊框／全息強度／稜鏡都不受影響。這是打包時的選項（環境變數 `AMBIENT_STARS=true/false` 可暫時覆蓋，測試用）；已經打包好的 exe 要重新打包才會套用。注意只有桌面掛件外殼有這個開關——卡片 html 用瀏覽器單獨打開時，星星一直都在。
- 常駐的星光／稜鏡動畫遵守 `prefers-reduced-motion`（多卡模式為了效能會強制模擬 reduce，所以那時星光是靜態的）；**使用者觸發的演出不受影響**，照常播放（跟自動翻面同一個道理）。
- 快捷鍵預設用 `Ctrl+Alt+數字`，不是 F2/F3/F5：全域快捷鍵會攔截整台電腦，單鍵會害你在別的程式裡失去重新命名／尋找下一個／重新整理；`Ctrl+Alt+R`／`M` 在這台機器上實測已被其他程式占用（`Ctrl+Alt+1/2/3` 是空的）。撞到時可在 builder-gui「⌨️ 快捷鍵」或 `package.json` 的 `skillKey`／`rarityKey`／`muteKey` 改；註冊失敗時主控台會印警告，系統匣選單永遠可用。
- 桌面掛件視窗設了 `autoplayPolicy: 'no-user-gesture-required'`——全域快捷鍵觸發時頁面沒有使用者手勢，不設的話 AudioContext 會卡在 suspended、完全沒聲音。

**運作方式**：`card-fx/fx.js`＋`fx.css`＋一段 `<script type="application/json" id="card-meta">` 被 `apply_fx.py` 包在 `<!--CARD-FX:BEGIN-->…<!--CARD-FX:END-->` 之間插進每張卡的 `</body>` 前面（卡片仍是單一自包含 html）。fx.js 只靠 `#card`、`.card__inner`、`.back__inner`、`.chip--ssr`、`.ghost--*` 這些既有結構，掛在 `window.cardFx`（`cast()`／`cycleRarity()`／`setRarity()`／`toggleMute()`），外殼的全域快捷鍵與系統匣選單就是呼叫它。

**改了 fx.js／fx.css 之後**，重新注入所有卡片：

```powershell
cd C:\cat\card-fx
C:\Users\homec\anaconda3\python.exe apply_fx.py          # 全部（cards/*.html；若 cards/dog_cards/ 底下還有 html 也會一起處理）
C:\Users\homec\anaconda3\python.exe apply_fx.py --check   # 只檢查誰不是最新
```

每張卡的**編號、收藏日期、四項數值**在 `card-fx/cards_meta.json`（第一次見到的卡自動建立：編號依序遞增、日期取檔案建立日、數值由名稱雜湊出穩定的 55～97；顏色／名稱／屬性／技能名從卡片 html 解析）。想調數值或日期，直接改這個 JSON 再執行一次 `apply_fx.py`；也能加 `"rarity": "UR"` 指定該卡的預設稀有度。**用「圖片一鍵生成卡片」做的新卡會自動注入**並登記編號，不需要再手動跑。

---

## 📤 匯出分享（WebM／GIF／PNG）

把卡片**旋轉一圈**錄成影片或動圖分享給別人，或匯出一張透明背景的 PNG。單卡、多卡都能用，桌面掛件 exe 打包後也能用。

| 格式 | 規格 | 大小／耗時（實測，jimmy 卡） | 說明 |
|---|---|---|---|
| **WebM** 影片 | VP9、1000×1360、30 fps、3 秒、6 Mbps | 約 1.6 MB／約 11 秒 | 畫質最好、檔案小。首尾相接可循環播放；有長度資訊、可拖曳進度 |
| **GIF** 動圖 | **750×1020**（`gifScale` 1.5，可選 0.8／1／1.5／2）、25 fps、3 秒、**每格獨立 255 色色盤＋誤差擴散** | 約 15～20 MB／約 40 秒（`gifScale`=1：約 8～10 MB） | 通用性最高（聊天軟體直接貼），但**檔案很大**；要傳給別人、檔案有大小限制就選 WebM 或把 GIF 尺寸調小。見下面〈GIF 畫質〉 |
| **PNG** 圖片 | 1000×1360、**透明背景** | 約 1 MB／約 2 秒 | 正面、全息光澤打出一點高光；同時複製到剪貼簿（可以直接貼上） |

**怎麼用**
- **快捷鍵** `exportKey`（預設 `Ctrl+Alt+4`）：用 `exportFormat`（預設 WebM）匯出。**多卡合一**時，匯出的是**滑鼠游標目前所在的那張卡**；游標不在任何卡片格位上（例如在螢幕邊緣、別的螢幕）就匯出**離游標最近的一張**，並在提示條上寫明「游標不在任何卡片上，改匯出最近的一張」——以前這種情況是什麼都不做、只有一則可能被 Windows 吞掉的通知，看起來就像功能失效。
- **系統匣選單「📤 匯出分享」**：可任選 PNG／WebM／GIF；多卡時每種格式底下列出各張卡片、「全部卡片（依序，各存一個檔）」、「🧩 整個版面（所有卡一起，合成一支）」，以及（WebM／GIF、3 張以上才有）「🕐 時鐘轉圈動畫」；底部有「📂 打開匯出資料夾」。
- **多卡：整個版面／時鐘轉圈**（2026-09-20，`exporter.js` 的 `exportStage()`，main.js 的 `runExportStage()`）：
  - **🧩 整個版面**（`Ctrl+Alt+5`／選單）：所有卡照桌面**目前**的座標與每張卡的縮放擺在同一個畫面，一起旋轉一圈（每張卡相位錯開一點點，有波浪感），輸出一支 WebM／GIF，或一張透明 PNG（PNG 會複製到剪貼簿）。秒數用 `exportSeconds`。檔名 `版面5張_layout_<時間>.webm`（PNG＝`版面5張_layout-still_…`）。
  - **🕐 時鐘轉圈動畫**（只在系統匣選單，WebM／GIF）：卡片沿 F8 同一組橢圓（`cyclePeriodSeconds` 秒繞一圈）移動，每張卡同時輕輕搖擺讓全息光澤流動，剛好繞完一圈可無縫循環。檔名 `版面5張_orbit_<時間>.webm`／`.gif`。
  - **做法**：離屏舞台頁面（每格一個 iframe 載入卡片，跟桌面掛件同架構），用 CDP 對每個 iframe 的執行環境逐格驅動角度（沿用單張匯出的 `PAGE_SCRIPT`，新增 `sway` 模式；`.stage` 支援 `--exp-scale` 做每張卡各自縮放），截出「所有卡的外框」。畫面尺寸依版面自動算：影片長邊 1600、GIF 長邊 560、PNG 長邊 2600（單張是 1000×1360）。
  - **進度顯示**：跟單張匯出一樣（畫面中央狀態面板＋系統匣＋通知），只是面板在螢幕中央、標題「📤 匯出中：整個版面（5 張）」。
  - **檔案大小／時間**（5 張，實測）：整個版面 WebM 約 1.6 MB／3 秒（約 40 秒）、GIF（長邊 900px）約 9～13 MB；時鐘轉圈 WebM 約 2～7 MB（約 1～2 分鐘）、GIF 約 25～30 MB／11 秒（12.5 fps）。動畫越長越大，GIF 比 WebM 肥很多，要傳給別人優先選 WebM。
  - 一樣受「同一時間只跑一個匯出」限制；匯出中再按會顯示目前進度。
- **GUI**（builder-gui）：「匯出分享」快捷鍵欄位與「匯出格式」下拉選單，跟其他快捷鍵一樣存進 `build/*.yml` 的 `extraMetadata`。
- **狀態面板（畫面內）**：按下快捷鍵後，**被匯出的那張卡正中央**會出現一個發光的面板——進行中＝**藍色**，標題「📤 匯出中：緋龍」＋進度條＋「錄製 32/75 · 43%」（載入約 3%、錄製 6～88%、編碼 90%）；完成＝**綠色**，寫出檔名、大小、存到哪個資料夾與「Ctrl+Alt+4 可再次匯出」，停留約 9 秒；失敗＝**紅色**＋原因（12 秒）。單卡就在視窗中央（＝卡片中央），多卡在該格位中央；滑鼠可以穿透它。匯出進行中再按一次快捷鍵，面板會停留約 3 秒顯示「⏳ 上一個匯出還沒結束　緋龍　目前 33%」（期間進度更新不會蓋掉這則提示），不會重複匯出。匯出「全部卡片」時標題帶「（第 2/6 張）」。這個面板不依賴 Windows 系統通知（會被專注輔助／通知設定吞掉）；系統匣通知仍保留，兩邊同時顯示。
- **系統匣的匯出狀態**：匯出中選單最上方會多一行「📤 匯出中：緋龍 43%」（每 10% 更新，結束後消失），提示文字也會帶進度；匯出區塊的標題直接寫出快捷鍵與預設格式，例如「── 📤 匯出分享（Ctrl+Alt+4＝匯出 GIF，多卡＝游標所在的卡）──」。
- **輸出位置**：`圖片\幻影卡匯出\<卡名>_spin_<日期時間>.webm`（PNG 是 `_still_`）。環境變數 `EXPORT_DIR` 可改資料夾。開始／完成／失敗都會跳系統通知，點完成通知會打開資料夾並選取檔案。 **換一台電腦時**：資料夾不寫在 yml／exe 裡，是**每次執行時才決定**——該電腦目前使用者的「圖片」資料夾＋`幻影卡匯出`（例如 `C:\Users\<名稱>\Pictures\幻影卡匯出`；若 OneDrive 接管了「圖片」資料夾，就會在 `OneDrive\圖片\幻影卡匯出`，你這台就是這樣）。不會跟著 exe 走、也不會同步到別台；用環境變數 `EXPORT_DIR` 可改成別的資料夾（目前沒有 GUI／yml 欄位）。完成面板與系統匣「📂 打開匯出資料夾」都會顯示／打開實際路徑。

**運作方式（為什麼不直接截桌面上那張卡）**：桌面掛件視窗有你自己的縮放／透明度／位置，穿透模式、被別的視窗蓋住、螢幕解析度不夠都會影響成品。所以匯出時**另開一個看不見的離屏視窗**重新載入同一份卡片 html（固定 500×680、卡片 372 寬置中，不受你桌面上的縮放／透明度影響），用 Chrome DevTools Protocol 的 `Page.captureScreenshot` 逐格截圖：
- 旋轉由匯出器接管，不跟卡片自己的動畫搶：所有卡片的 `.card` 都是 `transform: rotateX(var(--rx)) rotateY(var(--ry))`，匯出時改吃 `--exp-rx`／`--exp-ry`，每一格精準指定角度（緩入緩出：起點終點角速度為 0，正面各停留一下，循環播放不突兀）。**所以任何卡片（含之後「圖片一鍵生成」的新卡）不用改就支援**。
- 全息光澤、主體／背景視差是卡片依「滑鼠位置」算的，所以每格另外送一個合成的 `pointermove`（跟穿透模式追蹤游標同一招），位置隨相位繞一圈，光澤才會在旋轉中流動。
- 影片／GIF 沒有透明可用，背景是以卡片主題色打光的深色漸層（畫在頁面裡，陰影與混合模式才會正確合成）；PNG 是真透明。
- 匯出時強制 `prefers-reduced-motion: no-preference`（你的 Windows 常回報 reduce，會把星光／稜鏡整個關掉），成品是「完整效果」（唯一例外：小星星照「環境星光」設定，關閉時匯出也不會有）。稀有度沿用你目前選的（同一個 `file://` localStorage）。
- 編碼放在另一個隱藏視窗：WebM 用 WebCodecs `VideoEncoder`（VP9，不支援退 VP8）＋自己寫的最小 WebM 封裝（不用 MediaRecorder：它只能即時錄、沒有長度資訊）；GIF 見下面〈GIF 畫質〉。編碼過程不會卡住桌面掛件。

**實測踩到的坑（改這段程式前先看）**
- ⚠️ 視窗還沒載入任何頁面就掛 debugger 並送 `Emulation.*`，**整個主程序會無聲崩潰**（沒例外、沒訊息）→ 先 `loadURL('about:blank')` 再掛。
- ⚠️ 放在螢幕外（或完全透明）的視窗會被 Chromium「原生視窗遮蔽偵測」判成 `visibilityState==='hidden'`，`requestAnimationFrame` 停擺 → `exporter.js` 開頭用 `disable-features=CalculateNativeWinOcclusion`＋`disable-backgrounding-occluded-windows` 兩個旗標（必須在 app ready 前設定，所以 `main.js` 一開頭就 `require` 它）。`show:false` 的隱藏視窗則會被降到約 1 fps（每格 4 秒），所以是「顯示但放在螢幕外、不搶焦點、不進工作列」。
- ⚠️ `webContents.capturePage()` 實測會回傳**落後一格**的畫面，而且視窗尺寸被螢幕高度限制；CDP 截圖會強制重畫、可指定 `deviceScaleFactor`（PNG/WebM 2 倍、GIF 0.8 倍直接算出來，不是事後縮圖）。
- 測試用的腳本如果沒有 `window-all-closed` 處理，匯出視窗一關 Electron 就預設退出，錯誤訊息印不出來（真正的外殼有主視窗，不受影響）。
- 驗證方式：`ffprobe`／`ffmpeg -f null` 全部解碼通過（WebM：vp9、1000×1360、90 格、3.0 秒；GIF：75 格、3.0 秒），再用真的 exe 的快捷鍵跑一次。

**GIF 畫質（2026-09-20 重做，`gifquant.js`）**——使用者要求「做大一點並盡可能還原最佳畫質」。舊版的問題：一份**全域** 255 色色盤要同時服務卡片正面、背面與所有角度，色不夠用，紫／青漸層出現一圈一圈的色帶；4×4 Bayer 抖動把它打成格紋雜訊；量化前的畫面又是 JPEG q92（有區塊雜訊，量化後還逐格閃）。現在：
- **尺寸**：單張 750×1020（`gifScale` 1.5；舊版 400×544）。GUI「GIF 尺寸」選單（0.8／1／1.5／2，預設 1.5 不寫進 yml）→ yml `gifScale` → main.js `GIF_SCALE`（也可用環境變數 `GIF_SCALE`）。多卡版面／時鐘轉圈的長邊＝`gifScale`×600（預設 900；舊版 560）。⚠️ 這個欄位一定要走 GUI：GUI 存檔會把它不認得的 yml 欄位（例如手寫的 `exportSeconds`）丟掉。
- **擷取**：GIF 每一格改用 **PNG 無損**（影片仍是 JPEG q92）。
- **量化**：每一格自己的 255 色本機色盤（第 256 個索引留給透明）——6 位元直方圖＋加權 RGB（R3:G4:B2）中位切割＋k-means 修正；第二格起用上一格的色盤暖開機再跑幾輪 k-means（死群改放到誤差最大的顏色），色盤逐格平滑演變不跳色。**蛇形 Floyd–Steinberg 誤差擴散**（強度 0.7）取代 Bayer。
- **只重畫變動的像素**：每個像素記住上次顯示的原色，差 ≤ 1 階視為沒變→透明索引，只存變動的最小矩形；多卡的靜止背景幾乎不佔容量。
- **串流編碼**：每收到一格就編一格（不再把所有畫面存在記憶體、也不會最後才卡一大段），狀態面板顯示「編碼 37/75」的真實進度（`cx-progress`）。
- **實測**（同一組 75 格，對無損原圖）：SSIM 0.840→0.931、PSNR 32.5→35.6 dB，檔案 17.9→18.3 MB（同尺寸 750×1020），漸層由色帶變平滑（比對圖見測試流程：`ffmpeg -lavfi ssim/psnr`）。單張約 37 秒、整個版面約 50 秒。
- ⚠️ 播放速度：GIF 的格間隔是 1/100 秒的整數——25 fps＝4、12.5 fps（時鐘轉圈 GIF）＝8。GIF 有 256 色的先天上限，要最高畫質請選 WebM。

**限制**：只支援桌面掛件外殼（瀏覽器單獨開卡片沒有這個功能）；同一時間只跑一個匯出；影片固定繞 Y 軸一圈（要改秒數用 `exportSeconds`）。

---

## 🪶 卡片瘦身（WebP＋去掉重複）

每張卡是自包含的 html，圖片用 base64 內嵌（0.6～2.2 MB），而且：①場景背景／警戒殘影／沉睡殘影是**同一張 JPEG 重複內嵌 3 份**；②主體是一張很大的 PNG；③`cards/dog_cards/` 還有 6 個跟 `cards/` **逐位元組相同**的拷貝。`card-slim/slim.py` 把這些處理掉。

**實測成果**（同一份程式、同一批卡片，前後各量一次）

| 項目 | 瘦身前 | 瘦身後 |
|---|---|---|
| `cards/*.html` 11 張合計 | 11.8 MB | **3.0 MB（−74.6%）** |
| 例：緋龍／五条悟·茈／吉米 | 1.69／2.24／1.00 MB | 0.39／0.55／0.42 MB |
| `dog_cards/` 重複的 6 個 html | 6.3 MB | 移到備份（沒有任何 exe 用到它們） |
| 六狗 portable exe（兩次打包實測） | 96.39 MB | **94.18 MB（−2.2 MB，−2.3%）** |
| 卡片頁面載入時間 | 約 70～260 ms | 約 55～115 ms |

> [!NOTE]
> **exe 只縮了 2～3%**：exe 裡 Electron 執行環境就佔約 92 MB，而且 exe 本身的壓縮本來就會把「同一張圖重複 3 份」吃掉大半，所以卡片瘦身對 exe 的效果有限；主要的收穫是 `cards/` 資料夾（磁碟、備份、傳檔）與載入速度。想再縮 exe 得動 Electron 本體（例如 electron-builder 的 `electronLanguages` 只留需要的語系），這次沒動。

**做了什麼**（只動 `<img src="data:image/...">`，其餘一個字都不改）
1. 每張唯一的圖轉 WebP：帶透明度的（主體）`quality=95`、**alpha 無損**；不透明的（場景）`quality=80`。保留 ICC 色彩描述檔、套用 EXIF 方向；轉完沒有變小就保留原圖。**圖片寬高完全不變**（卡片版面靠 `object-fit`／位置計算，尺寸變了會跑位）。
2. 內容相同的 `<img>` 只留第一份的 base64（加 `data-slim-id`），其餘的 `<img>` 改成 `data-slim-from="s1"`，並在最後一個被改的 `<img>` 後面插一段同步的 `<script id="card-slim-resolve">`，在首次繪製前把 `src` 補回去（複製的是同一個字串，不會多一份 base64；`fx.js` 技能演出的 `cloneNode` 殘影也會帶著 `src`）。**手動改卡片時請保留那段 script。**
3. 自我檢查：寫檔前確認每張圖都解得開、每個 `data-slim-from` 都有來源、resolver 只有一份且位置在所有相關 `<img>` 之後；沒過就**不寫**。

**用法**
```powershell
cd C:\cat\card-slim
python slim.py --dry-run          # 只看會省多少，不寫檔
python slim.py                    # 處理 ../cards/*.html；原檔先備份到 card-slim\backup\<時間>\
python slim.py a.html b.html      # 只處理指定檔案
python slim.py --out D:\test    # 不動原檔，結果寫到別的資料夾（測試用）
```
可重複執行（已是 WebP／已收成一份的不會再動）。**用「圖片一鍵生成」做的新卡會自動瘦身**（`make_card.py` 在組完 html 後呼叫同一份 `slim.py`，失敗時退回沒瘦身的版本，不會讓生成失敗）。`cards/dog_cards/build/` 那份六狗食譜重新生成出來的卡是沒瘦身的，生成後再跑一次 `slim.py <檔案>` 即可。

**還原**：備份在 `card-slim/backup/<時間>/`（`cards/*.html` 的原檔）與 `card-slim/backup/dog_cards_dupes/`（原本的 6 個重複檔），把檔案複製回 `cards/`（或 `cards/dog_cards/`）就是原樣。確認沒問題後整個 `card-slim/backup/` 可以刪掉（約 18 MB，不會被打包進任何 exe，也不會被 GUI 掃到）。

**驗證方式**（先在獨立沙盒副本上做完全部測試，全部通過才套用到正式目錄，並確認正式產物與沙盒產物逐位元組相同）
- **畫面比對**：11 張卡各 4 種姿勢（正面／17°／背面／306°），原版 vs 瘦身版關閉環境動畫後逐像素比：PSNR **≥ 47.8 dB**，背面完全相同（99 dB）；每張卡每個 `<img>` 的自然尺寸、解碼狀態逐一比對，**全部一致**。差異最大的是「營區陽光男孩」的 4 個孤立像素（原圖上幾乎全透明的雜點，共約 34 萬像素），肉眼看不出。
- **功能**（真實 OS 按鍵，沙盒外殼、正式外殼各跑一次）：單卡×4 種架構、六狗合一、混合四卡合一——圖片載入、殘影層還原、`Ctrl+Alt+1` 技能演出（含爆發時複製的殘影層有圖）、`Ctrl+Alt+2` 稀有度、`F4` 自動翻面、`Ctrl+Alt+4` 匯出 GIF（可解碼、75 格），正式外殼共 82 項全過（沙盒外殼與打包後的 exe 各另跑一輪，也全過）。
- **打包**：沙盒打包出六狗 exe，用 exe 本體跑同一套多卡測試 29/29 通過。
- **相容**：`apply_fx.py --check`／重新注入後 md5 不變；GUI 正常列出 11 張卡（顯示的是瘦身後大小）；`make_card.py` 生成一張新卡（497 KB → 152 KB）與未瘦身版渲染比對 PSNR 55.6 dB。
- 既有的 `dist/六狗.exe`、`吉米.exe`、`luffy.exe` **是舊的**，要用 GUI 重新打包才會用到瘦身版。

---

## 🪄 圖片一鍵生成卡片

在 `builder-gui` 最上面的「🪄 圖片一鍵生成卡片」面板：**拖入（或點擊選擇、Ctrl+V 貼上）一張圖片 → 按「生成卡片」**，約 5～30 秒後在 `cards/` 多出一份 `<名稱>_phantom_card.html`，同時出現在下方的卡片清單（單卡選單、多卡格位下拉都會更新），可以直接拿去打包。面板內建預覽（可拖曳旋轉、雙擊翻面）與主色色票。

**它做了什麼**（`card-maker/make_card.py`）：

1. **去背**：rembg（`isnet-anime` 動漫角色／`isnet-general-use` 照片與動物／或「不去背」＝整張圖橢圓羽化當主體）；軟化 alpha、去掉零星孤島、裁到緊貼邊界。**圖片本身已有透明背景就自動跳過去背。** 太瘦高的全身像只保留上半身（高寬比上限 1.3），底部漸層淡出融進技能面板。
2. **抽主色**：對主體像素做加權色相直方圖（飽和度 × 亮度），取最強色相 h1 當主色、次強且相隔 ≥40° 的當副色 h2（沒有明顯第二色就取分裂互補 h1+150°）；畫面偏灰時退回冷色調預設。
3. **推導整組配色**：`:root` 的 `--c1/--c2/--c3/--abyss`、body／內框背景漸層、飄塵、兩層殘影的 `sepia + hue-rotate` 濾鏡（把殘影染成主/副色）、**全息色階 `HOLO_STOPS`**（主副色與鄰近色相交錯的 6 段稜鏡）、屬性晶片、技能面板、名稱/技能發光、卡背色系。金屬外框沿用系列統一的金色。
4. **排版**：依主體高寬比算主體寬度／位置；用 `object-position` 讓場景窗對準主體（CSS `cover` 的百分比不是絕對位置，這裡有換算）。
5. **全息背景效果**依主色自動挑（暖色→餘燼、綠→粒子、藍→雷達、紫粉→網格、灰→掃光），也可手動指定。
6. 輸出跟六狗系列**同架構**：360° 環轉、卡背（徽章環＋屬性晶片）、雙擊翻面、`toggleAutoFlip`（L／桌面掛件 F4）、hover 視差，並自動注入〈✨ 卡片特效〉（技能演出／稀有度切換／卡背資訊／音效），所以生成完直接能在桌面掛件裡用，不需要再改。面板選的稀有度就是這張卡的初始稀有度。

**欄位**都可留空（名稱預設用檔名、技能名稱/說明/屬性/卡背字/小標題自動推導）；技能說明可用 `**粗體**`；重名預設拒絕（不弄丟舊卡），勾「覆蓋同名檔案」才會覆蓋。**檔名只保留文字（含中日韓）、數字、`_`、`-`，其他字元（`% # ? & [ ] ( ) { } ! *`、空白…）一律換成 `_`**——檔名會被當成網址、`electron-builder` 的 `files` 萬用字元和 yml 的值；卡片上顯示的名稱不受影響。（曾經發生過名稱含 `%` 的卡片打包後視窗空白：`win.loadFile()` 把 `%3F` 當成網址編碼。外殼現在改用 `pathToFileURL` 載入，含特殊字元的舊檔名也能開，但新卡還是會先被清乾淨。）中間產物（`subject.png`／`scene.jpg`／`config.json`，含所有推導出的 token）留在 `card-maker/output/<名稱>/` 方便日後手動微調。

**環境需求**：本機 Python + `pillow numpy scipy rembg[cpu]`。GUI 會依序找 `CARD_PYTHON` 環境變數、`%USERPROFILE%\anaconda3\python.exe`、`miniconda3`、`C:\ProgramData\anaconda3`、最後才是 PATH 的 `python`。第一次用某個去背模型 rembg 會下載約 170MB 到 `~\.rembg\models\`（面板會顯示「正在下載」）。也可以單獨命令列用：`python card-maker\make_card.py --params params.json`（欄位見檔案開頭的說明）。

> [!NOTE]
> 自動推導的是「一個好的起點」，不是美術判斷：主色抽錯（例如背景很搶眼）時，換一種去背方式或直接改輸出的 html `:root` 裡的 `--c1/--c2/--c3` 即可；`card_template.py` 是模板本身（跟 `cards/dog_cards/build/template.py` 同源，卡背改成最新版樣式並新增 `%%RARITY%%`），要改所有新卡的共同外觀改那份。

---

## ➕ 怎麼加一張新卡片

> [!TIP]
> 不想手動編輯 yml：`npm run builder-gui`（或雙擊 `啟動html白名單管理.bat`）會開一個
> 小視窗。單卡模式選一張要載入的 html，還能填「開機預設位置」（x/y，相對螢幕
> 正中央的偏移量，對應 `extraMetadata.defaultOffset`，只在這個 exe 第一次啟動、
> 還沒被拖過時生效，之後拖過一次就改存實際位置）——同時開好幾個單卡 exe 想
> 一開始就不疊在一起，直接在這裡設定就好，不用開起來後再手動拖開；多卡模式是
> 「格位編輯器」——每一列
> 明確指定「這一格要放哪張卡、貼在哪個座標」，可以直接指定/交換某張卡放在某個
> 位置（例如 `ten-card-grid.yml` 那種固定網格，想換掉某一格的卡片就直接在那一列
> 改下拉選單，其他格不受影響），也能「➕ 新增格位」／「🔲 全部重新排成網格」。
> 存檔時自動算好 `files` 白名單跟 `cardFile`／`cards`，保證兩邊一致，不用自己
> 對照；每份設定旁邊還有「📦 打包成 exe」按鈕，直接在畫面上跑
> `electron-builder`，即時看輸出，打包完直接開資料夾拿 exe，不用開終端機；
> 也有「🗑️ 刪除」按鈕清掉不要的設定（會先跳確認、刪除前也會自動備份到
> `build/.bak/`），不用手動去 `build/` 資料夾找檔案砍。
> ⚠️ 用這個 GUI 存檔會把該份 `.yml` 換成簡短的自動產生註解，原本手寫的詳細
> 說明不會留著（存檔前會自動備份到 `build/.bak/`，C:\cat 沒有 git，這是唯一的
> 復原手段）——只是想加減卡片、想保留說明文字的話，還是照下面手動編輯。

**單卡版本**：先把新卡片的 html 放進 `cards/` 資料夾，再到 `build/` 複製一份現有的單卡 `.yml`（例如 `card-ez62.yml`，目前是吉米那份），改：

```yaml
extends: electron-builder.yml
productName: <新卡名>-card
files:
  - cards/<新卡html檔名>.html   # 白名單：一定要加這行，不然新卡片不會被打包進 exe
                                # （見 electron-builder.yml 開頭「files 是聯集不是
                                # 取代」的說明）
extraMetadata:
  name: <新卡名>-card-desktop   # 讓 userData（存檔）跟其他卡分開，避免共用一個設定目錄
  cardFile: cards/<新卡html檔名>.html
  toggleKey: F12                # 如果會跟其他同時開著的單卡 exe 衝突，換一個沒用過的鍵
```

打包：`npx electron-builder --config build/<檔名>.yml`

**併入現有的多卡網格**：把新卡片的 html 放進 `cards/`，改 `build/card-ehai.yml`（六狗那份，或其他多卡設定）`cards` 陣列裡對應格子的 `file`（記得帶 `cards/` 前綴），`x`/`y` 不用動——見下一節的網格座標。**如果這個新檔名還沒出現在同一份 `.yml` 的 `files` 白名單裡，記得也加進去**，不然畫面上那個格子會是空的（html 沒被打包進 exe），重新打包即可。

---

## 📐 網格座標系統（目前的 10 卡佈局）

> [!NOTE]
> `build/ten-card-grid.yml` 這個檔名目前不存在於 `build/` 底下（可能已被刪除或改名，見〈檔案結構〉）——這節留著純粹是座標**算法**還有參考價值，之後要排新的多卡網格可以套用同一套外推邏輯，不代表現在還有一份叫這個名字的設定檔可以直接打包。

`ten-card-grid.yml` 的座標不是隨便喬的，是從使用者實際手動拖曳、確認滿意的排列**精算外推**出來的（過程見對話紀錄）：

- 兩個基準點：左上角第一欄 `x = -89`，右上角最後一欄 `x = 1430`（都是 `.card-slot` 這個 560×820 格子的左上角座標，不是卡片本體的座標——卡片本體在格子裡往內縮約 94px、往下縮約 152~162px）。
- **欄（水平）**：5 欄等分 4 個間隔，`(1430 - (-89)) / 4 = 379.75px` 一格：`-89 / 291 / 671 / 1050 / 1430`。
- **列（垂直）**：上排 `y = -130`、下排 `y = 402`，間距 532px（**10 卡網格**目前排出來的值；舊版是 `-153`／`379`）。
  - ⭐ **「一鍵排成 10 卡（上五下五）」按鈕**（2026-09-20，GUI 多卡編輯區，「排成 6 卡」旁邊）：一按就把格位補／裁到剛好 10 格並排好——**邏輯跟 6 卡一樣**：中間欄固定 `x = 671`、上排 `y = -136`、下排 `y = 396`（列距 532），上下兩排逐欄左右對齊；`x = -89／291／671／1051／1431`（欄距 380）。欄距不用 6 卡的 570，因為 5 欄用 570 會排出 2900px 寬、超出 1920 的螢幕；380 剛好讓卡片本體落在 5～1897px、彼此貼齊不重疊。不夠 10 格時新增的格位**優先放還沒用過的卡片**（依 cards/ 清單順序，用完才重複最後一格），已經有的格位卡片不動；超過 10 格會裁掉多的；可重複按（結果一樣）。按完照樣可以在下面逐格改卡片、微調座標。10 格也能用 F8 時鐘循環（每張間隔 36° 沿橢圓轉，實測 rx=760、ry=266）與 `Ctrl+Alt+5` 匯出整個版面；F7 手勢任何張數都能用。舊的「🔲 全部重新排成網格」（5 欄、y＝-130／402）沒有動。
  - ⭐ **「排成 6 卡」的預設座標＝你親手調到最滿意的六狗設定（2026-09-20）**：`x = 101／671／1241`、上排 `y = -136`、下排 `y = 396`（列距 532），GUI 的 `SIX_CARD_BASE` 直接寫死這六組數字（縮放比例不是 100% 時等比例縮放）。系統匣選單的「重設卡片位置」（`F11`）本來就是回到**打包時 `build/*.yml` 裡的座標**，所以打包這份 yml 之後，「排成 6 卡」與「重設卡片位置」會是同一組。10 卡網格的 `-130` 沒有跟著改（兩者只差 6px）。
  - **卡片本體在格位裡的位置（實測）**：左＝94、上＝**143.6px**（不是貼齊格位頂端）。`.stage` 高 652.8＝上邊距 60＋卡 520.8＋間距 14＋提示文字 2 行 32＋下邊距 26，在 820 的格位裡垂直置中（頂端 83.6），再加上邊距 60 ＝ 143.6。所以兩排卡片本體的螢幕座標是 `y＝格位y＋143.6`：`-130` → 上排 13.6～534.4、下排 545.6～1066.4，在 1080 高的螢幕上下各留約 13.6px，剛好置中。
  - **為什麼從 `-153` 改成 `-130`**：`-153` 是提示文字（卡片下方那行「移動滑鼠 · 傾斜裝置 · …」）還只有 1 行時調的，那時卡片本體在格位頂端＋約 153，剛好貼齊螢幕上緣；後來提示文字加長、折成 2 行，`.stage` 變高、卡片往上移到 143.6，用舊座標上排會凸出螢幕上緣約 10px、下面多空 36px。**你已經存好的 `build/*.yml` 不會被改**，想套用新預設：GUI 編輯該設定 → 按「排成 6 卡（上三下三）」→ 儲存 → 重新打包。
  - **GUI 的預覽以前畫錯**：預覽把卡片畫在格位頂端＋60（當成 `.stage` 貼齊頂端），比真實位置高了 84px，看起來像上排卡片大幅凸出藍色「螢幕邊界」；現在改用 143.6（隨縮放比例等比例），實測 GUI 預覽 vs 真實外殼在 80%／100%／120% 縮放下誤差都在幾 px 內（量測時多出的 3px 是預覽 `.screen-boundary` 的邊框）。**143.6 不再依賴提示文字折幾行**：外殼把 `.hint` 的高度固定成 32px（`CARD_SCALE_CSS`，overflow 可見），提示文字之後怎麼加長（例如補上匯出快捷鍵）都不會改變 `.stage` 高度、卡片位置不會跑；11 張卡逐張實測加長提示前後 `.stage` 高度與卡片位置完全一致。
- 這個間距故意小於格子本身的寬高（560/820），因為格子裡有很大一圈是給金屬外框陰影暈染用的透明留白，卡片本體（372×521）沒那麼大——用這個間距排出來，相鄰卡片的**可見卡面**只留約 7~11px 空隙，緊貼但不重疊，格子的透明部分則會小幅互相蓋住（反正是透明的，看不出來）。

> [!NOTE]
> 這一套 pitch 是量測**這台螢幕在目前卡片尺寸（1920×1080、卡片最大寬 372px）** 下、使用者親自排列後回推的結果，換了螢幕解析度或哪天卡片的 CSS 尺寸被調整過，需要重新測量，不是可以直接套用到任何情境的通用公式。

---

## 🏗️ 建置指令

```powershell
# 開發預覽（用 package.json 目前的 cardFile/cards，見〈npm start 快速切換〉可暫時覆蓋）
cd C:\cat
npm start

# 包裝某個特定版本（單卡或多卡都一樣的指令形式，檔名以 build/ 底下「目前」
# 實際存在的 .yml 為準——見〈檔案結構〉，builder-gui 存檔會改檔名）
npx electron-builder --config build/card-ez62.yml   # 單卡：吉米
npx electron-builder --config build/card-ehai.yml   # 多卡：六狗
```

> [!WARNING]
> `package.json` 的 `dist` 指令目前寫死指到 `build/jimmy-phantom.yml`，但這個檔案已經不存在（被 builder-gui 改名成 `card-ez62.yml`）——**現在直接 `npm run dist` 會失敗**。要嘛照上面用 `npx electron-builder --config build/<檔名>.yml` 指定實際存在的檔名，要嘛把 `package.json` 的 `dist` 腳本改成指向 `build/card-ez62.yml`（或你當下想預設打包的那份）。

輸出都在 `dist/<productName>.exe`（portable，單一檔案，不用安裝）。

---

## 🩹 疑難排解

**卡片跑到螢幕外面、看不到了 / 想復原成初始位置**：系統匣圖示右鍵選單有「↩️ 重設卡片位置」，或直接按 `F11`（或該建置設定的 `resetKey`），點下去/按下去都會先跳確認視窗，按「重設」就會把卡片（單卡：視窗本身；多卡：所有格位）搬回預設位置，不用自己找檔案刪——多卡模式這個動作會整個 reload 視窗，F7/F8 的開關狀態也會一起回到剛啟動的樣子。

這個選項背後做的事，就是手動刪掉下面這兩個存檔（該 exe 的 userData 資料夾，`name`/`extraMetadata.name` 決定資料夾名稱）——選單裡那個問題求助不到、或想直接手動操作時可以照這樣做：

```powershell
# 單卡版本：刪了會回到 defaultOffset 算出來的置中位置
Remove-Item "$env:APPDATA\<name>\window-position.json"

# 多卡版本：刪了會回到 build/*.yml 裡 cards[].x/y 的預設格子位置
Remove-Item "$env:APPDATA\<name>\card-slots.json"
```

**多卡版本右鍵拖曳完全沒反應，但滑鼠移過去卡片還是會轉**：代表卡片自己的 `<script>` 有跑（那是它原本就有的效果），但外殼注入的 `window.cardShell` 沒送進 iframe——檢查 `main.js` 建立多卡視窗那段 `webPreferences` 有沒有 `nodeIntegrationInSubFrames: true`（這是目前唯一已知會讓這個功能整個失效的設定漏掉的地方）。

**同時開兩個「單卡」exe，`F9`/`F10` 只有一個有反應，甚至後開的那個看起來關不掉**：這是 OS 全域快捷鍵的限制，同一個按鍵只能被一個程式搶到，先開的那個 exe 會把鍵搶走，後開的全部失敗（不只 `F9`，`F10` 結束程式也會一起失敗）。給每個單卡建置設定不同的 `toggleKey`/`quitKey`（見〈怎麼加一張新卡片〉，builder-gui 的「⌨️ 快捷鍵」欄位可以直接改），或者改用多卡合一模式（整個視窗共用一組快捷鍵，沒有這個問題）——這是治本的辦法。
治標／保底：每個 exe 啟動時都會建立一個系統匣（工作列右下角）圖示，不管快捷鍵有沒有搶到，右鍵點這個圖示一定找得到「結束程式」跟「切換 互動/穿透」，不會再有「開了但真的關不掉，只能開工作管理員」的情況；快捷鍵搶不到時，圖示的滑鼠停留提示文字也會多一行警告。

---

## 📋 目前已有的建置設定 Cheat Sheet

> [!NOTE]
> 這份清單只列 `build/` 底下**目前實際存在**的 `.yml`。builder-gui 每次存檔都可能把檔名換成新的隨機四碼後綴（見〈檔案結構〉），下面的檔名之後可能又不一樣了——真的要用哪份，開 `npm run builder-gui` 看畫面上列出來的，或直接 `ls build/*.yml` 確認最準。

| 檔案 | 內容 | `toggleKey` |
|---|---|---|
| `build/card-ez62.yml` | 單卡：吉米 | F9 |
| `build/card-ehai.yml` | 多卡：六狗（feilong/wangcai/wuming/yinglang/zhuaige/jifeng），F7 手勢任何張數都能用、F8 時鐘循環 3 張以上都能用 | F9 |
